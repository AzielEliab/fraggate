FragGate verifies kernels in the background.
Author: Aziel Eliab

1. python -m venv .venv && source .venv/bin/activate && pip install -e .
2. fraggate
3. fraggate ping

Also: fraggate doctor
Background listener: fraggate service
Status: fraggate service status  (Running or Quiet)

Machines: add --json
